services:
  llm-starter:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/project
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
    stdin_open: true
    tty: true
